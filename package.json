{
 "workspaces": [
  "packages/*"
 ],
 "scripts": {
  "build": "lerna run build --stream --parallel",
  "test": "jest"
 },
 "devDependencies": {
  "jest": "^23.6.0",
  "lerna": "^3.4.1",
  "parcel-bundler": "^1.10.1",
  "react-test-renderer": "^16.5.2"
 },
 "name": "demo-monorepo",
 "private": true
}
